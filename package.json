{
  "name": "conway",
  "main": "server/index.js",
  "dependencies": {
    "express": "^4.17.1",
    "socket.io": "^2.3.0"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^7.1.1",
    "standardx": "^5.0.0"
  },
  "scripts": {
    "start": "node .",
    "lint": "npx --no-install standardx || cd .",
    "fix": "npx --no-install standardx --fix || cd .",
    "test": "npx --no-install mocha"
  },
  "license": "UNLICENSED",
  "private": true
}
