{
  "name": "socketio-gol",
  "main": "server/index.js",
  "engines": {
    "node": "12.16.x"
  },
  "dependencies": {
    "express": "^4.17.1",
    "loglevel": "^1.6.7",
    "loglevel-plugin-prefix": "^0.8.4",
    "pm2": "^4.2.3",
    "random-color": "^1.0.1",
    "socket.io": "^2.3.0"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^7.1.1",
    "nodemon": "^2.0.2",
    "standardx": "^5.0.0"
  },
  "scripts": {
    "start": "pm2-runtime start pm2.json --env production",
    "nodemon": "nodemon --ignore ./public/ ./server/index.js",
    "lint": "standardx --verbose || cd .",
    "fix": "standardx --verbose --fix || cd .",
    "test": "mocha ./server/test/*.test.js"
  },
  "license": "UNLICENSED",
  "private": true
}
